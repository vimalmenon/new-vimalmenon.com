orbs:
  node: circleci/node@4.2.0

version: 2.1

jobs:
  test:
    docker:
      - image: circleci/node:8-browsers
    steps:
      - checkout
      - node/install-packages
      - run: npm install
      - run: npm run test

workflows: 
  test_and_deploy:
    jobs: 
      - test