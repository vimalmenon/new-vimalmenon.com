import contactUs from "./contact-us";
import hero from "./hero";

const earlyRelease = "This website is in early release (alpha), so some features might not complete or cemented. Please be aware that some pages might be missing or some features might not work as expected.";

export default {
	earlyRelease,
	contactUs,
	hero
};